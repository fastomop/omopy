"""OMOPy — Pythonic, type-safe interface for OMOP CDM databases.

Submodules:
    - ``omopy.generics`` — Core type system (Phase 0)
    - ``omopy.connector`` — Database CDM access (Phase 1, future)
    - ``omopy.vis`` — Visualisation (Phase 2, future)
    - and more...
"""

__version__ = "0.1.0"
